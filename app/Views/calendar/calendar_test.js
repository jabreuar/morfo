'use strict';

describe('morfo.calendar module', function() {

  beforeEach(module('morfo.calendar'));

  describe('calendar controller', function(){
    it('should ....', inject(function($controller) {
      //spec body
      var view1Ctrl = $controller('CalendarCtrl');
      expect(view1Ctrl).toBeDefined();
    }));

  });
});