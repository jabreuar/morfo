'use strict';

describe('morfo.login module', function() {

  beforeEach(module('morfo.login'));

  describe('login controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view2Ctrl = $controller('LoginCtrl');
      expect(view2Ctrl).toBeDefined();
    }));

  });
});